import { SearchpackPipe } from './searchpack.pipe';

describe('SearchpackPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchpackPipe();
    expect(pipe).toBeTruthy();
  });
});
